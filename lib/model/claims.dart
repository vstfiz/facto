class Claims {
  String requestedBy;
  String date;
  String news;
  String status;
  String factCheck;
  bool isSelected;
  String url1;
  String url2;

  Claims(this.requestedBy, this.date, this.news, this.status, this.factCheck,this.isSelected);
}
